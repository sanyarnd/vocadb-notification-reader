package vocadb.notification.reader.service.dto.notification;

public class EventNotification extends Notification {
    public EventNotification(Integer id, String originalSubject, String originalBody) {
        super(id, originalSubject, originalBody);
    }
}
